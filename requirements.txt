# Core (required for the LLM path; the code runs without them too, see README)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0

# Optional, used only if you re-run the analysis / plots in RESULTS.md
pandas>=2.0.0
matplotlib>=3.8.0
